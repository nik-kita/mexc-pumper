import dotenv from 'dotenv';

dotenv.config();

(async () => {
    try {
        console.log('indes.ts');
    } catch (error) {
        console.log('======================== ERROR ==========================');
        console.log(error);
    }
})();
